version: "3"

services:
  react:
    build:
      context: ./
      dockerfile: ./env/docker/Dockerfile
    container_name: sig-react
    volumes:
      - .:/react
    ports:
      - 3000:3000
    tty: true